describe pip('certifi') do
    it { should be_installed }
    its('version') {should eq'2018.10.15'}
  end
describe pip('chardet') do
    it { should be_installed }
    its('version') {should eq'3.0.4'}
  end
describe pip('coreapi') do
    it { should be_installed }
    its('version') {should eq'2.3.3'}
  end
describe pip('coreschema') do
    it { should be_installed }
    its('version') {should eq'0.0.4'}
  end
describe pip('Django') do
    it { should be_installed }
    its('version') {should eq'1.11.16'}
  end
describe pip('django-rest-framework') do
    it { should be_installed }
    its('version') {should eq'0.1.0'}
  end
describe pip('djangorestframework') do
    it { should be_installed }
    its('version') {should eq'3.9.0'}
  end
describe pip('djangorestframework') do
    it { should be_installed }
    its('version') {should eq'3.9.0'}
  end
describe pip('idna') do
    it { should be_installed }
    its('version') {should eq'2.7'}
  end
describe pip('itypes') do
    it { should be_installed }
    its('version') {should eq'1.1.0'}
  end
describe pip('Jinja2') do
    it { should be_installed }
    its('version') {should eq'2.10'}
  end
describe pip('MarkupSafe') do
    it { should be_installed }
    its('version') {should eq'1.1.0'}
  end
describe pip('Pygments') do
    it { should be_installed }
    its('version') {should eq'2.2.0'}
  end
describe pip('pytz') do
    it { should be_installed }
    its('version') {should eq'2018.7'}
  end
describe pip('requests') do
    it { should be_installed }
    its('version') {should eq'2.20.0'}
  end
describe pip('uritemplate') do
    it { should be_installed }
    its('version') {should eq'3.0.0'}
  end
describe pip('urllib3') do
    it { should be_installed }
    its('version') {should eq'1.24.1'}
  end
